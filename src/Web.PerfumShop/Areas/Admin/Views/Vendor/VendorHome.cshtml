@using Web.PerfumShop.Areas.Admin.Models
@model VendorList

@{
    ViewBag.Title = "VendorHome";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>VendorHome</h2>

<p>
    @Html.ActionLink("Create New Vendor", "Create")
</p>
<table class="table">
    <tr>
        column
    </tr>

@foreach (var item in Model.VendorViewModels) {

    Html.RenderPartial("_VenderIdem", item);

}

</table>
